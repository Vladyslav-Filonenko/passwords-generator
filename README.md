# Password Generator

CLI password generator with customizable options.

## Features
- Choose character types: digits, uppercase, lowercase, symbols
- Guaranteed at least one character of each selected type
- Save passwords to file or print to terminal
- Cryptographically secure (uses `secrets` module)

## Usage
```bash
python3 password_generator.py
```